apiVersion: v1
kind: Service
metadata: 
    name: nginx-dnbo
    namespace: dnbo-test
spec: 
  type: ClusterIP
  ports: 
    - port: 3000 #allow other pods access to this pod
      targetPort: 3000 #the target port of the containter
  selector: 
    app: nginx-dnbo


  